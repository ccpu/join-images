# Changesets

This folder contains release notes used by Changesets.

Create a changeset for any user-facing package change by running:

```bash
pnpm changeset
```

When a release is ready, the release workflow consumes these files to publish the package and update version history.